### `<%- item.name -%>`
**<%= desc.translations.vulnerabilityDescription %>**：
<% item.problems.forEach((problem) => { %>- <%- problem.title %>
  - <%= desc.translations.npmVulnerabilityId %>：`<%- problem.source %>`
  - <%= desc.translations.vulnerabilityDetails %>：[<%- problem.url %>](<%- problem.url %>)
  - <%= desc.translations.vulnerabilitySeverity %>：<%- desc.severityLevels[problem.severity] %>
  - <%= desc.translations.affectedVersions %>：`<%- problem.range %>`
<% }); %>
**<%= desc.translations.dependencyRelationship %>**：
<% if(item.depChains.length === 0) { -%>
<% if(item.name === packageJson.name) { -%><%- desc.translations.currentProject %>
<% } else { -%>- `<%- packageJson.name %>/<%- item.name %>`
<% } -%>
<% } else { -%>
<% item.depChains.forEach((chain) => { -%>
<% if(chain.length === 1 && chain[0] === packageJson.name) { -%><%- desc.translations.currentProject %>
<% } else { -%>- `<%- packageJson.name %>/<%- chain.join('/') %>`
<% } -%>
<% }); -%>
<% } -%>
**<%= desc.translations.vulnerabilityPackageDirectory %>**：
<% item.nodes.forEach((path) => { %>- `<%- path %>`
<% }); %>
<% if (item.fixAvailable) { %>**<%= desc.translations.fixRecommendation %>**：
<% if (item.fixAvailable.version) { %>
- <%= desc.translations.updateToVersion %> `<%- item.name %>` <%= desc.translations.toVersion %> `<%- item.fixAvailable.version %>`
<% if (item.fixAvailable.isSemVerMajor) { %>- <%= desc.translations.noteSemverMajor %>
<% } %>
<% } else { %>
- <%= desc.translations.updateToVersion %> `<%- item.name %>` <%= desc.translations.updateToLatestVersion %>
<% } %>
<% } else { %>
**<%= desc.translations.fixRecommendation %>**：
- <%= desc.translations.noAutoFixAvailable %>
<% } %>