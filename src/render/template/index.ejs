# `<%- packageJson.name %>`审计结果

## 检测目标
<% if (targetPath.startsWith('http://') || targetPath.startsWith('https://')) { %>
**链接地址:** <%- targetPath %>
<% } else { %>
**文件路径:** `<%- targetPath %>`
<% } %>
<% if(audit.summary.total) { %>
<%- include('./audit.ejs'); %>
<%- include('./recommendations.ejs'); %>
<% } %>
<% if(audit.summary.total === 0) { %>
你项目的所有直接依赖和间接依赖都没有发现任何风险漏洞。

## 依赖管理建议

### 保持现状
- 项目当前的依赖管理状况良好，没有发现任何风险漏洞
- 建议继续保持当前的依赖管理实践

### 持续改进
1. **定期审计**：建议至少每月运行一次依赖审计，确保新发现的漏洞能够及时处理
2. **更新依赖**：定期更新依赖到最新的安全版本
3. **监控依赖**：考虑使用依赖监控工具，及时获取依赖的安全更新通知
<% } %>
