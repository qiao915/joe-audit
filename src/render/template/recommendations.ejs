## <%- desc.translations.dependencyManagementRecommendations %> / <%= desc.translations.dependencyManagementRecommendationsEn %>

### <%- desc.translations.fixPriorityRecommendations %> / <%= desc.translations.fixPriorityRecommendationsEn %>
<% if (audit.summary.critical > 0) { %>
1. **<%= desc.translations.fixCriticalImmediately %>**：
   - <%= desc.translations.criticalImpact %>
   - <%= desc.translations.fixCriticalWithin24h %>
<% } %>
<% if (audit.summary.high > 0) { %>
2. **<%= desc.translations.fixHighAsSoonAsPossible %>**：
   - <%= desc.translations.highImpact %>
   - <%= desc.translations.fixHighWithin7d %>
<% } %>
<% if (audit.summary.moderate > 0) { %>
3. **<%= desc.translations.fixModeratePlanned %>**：
   - <%= desc.translations.moderateImpact %>
   - <%= desc.translations.fixModerateInNextRelease %>
<% } %>
<% if (audit.summary.low > 0) { %>
4. **<%= desc.translations.fixLowEvaluate %>**：
   - <%= desc.translations.lowImpact %>
   - <%= desc.translations.fixLowBasedOnSituation %>
<% } %>

### <%- desc.translations.dependencyVersionManagement %> / <%= desc.translations.dependencyVersionManagementEn %>

1. **<%= desc.translations.useFixedVersion %> / <%= desc.translations.useFixedVersionEn %>**：
   - <%= desc.translations.avoidTildeCaret %>
   - <%= desc.translations.useExactVersion %>

2. **<%= desc.translations.updateDependenciesRegularly %> / <%= desc.translations.updateDependenciesRegularlyEn %>**：
   - <%= desc.translations.useNpmOutdated %>
   - <%= desc.translations.useNcuTool %>
   - <%= desc.translations.updateForSecurity %>

3. **<%= desc.translations.removeUnusedDependencies %> / <%= desc.translations.removeUnusedDependenciesEn %>**：
   - <%= desc.translations.checkAndRemove %>
   - <%= desc.translations.reduceDependencyCount %>

4. **<%= desc.translations.useLockFiles %> / <%= desc.translations.useLockFilesEn %>**：
   - <%= desc.translations.includeLockFiles %>
   - <%= desc.translations.lockFilesBenefit %>

### <%- desc.translations.securityAuditRecommendations %> / <%= desc.translations.securityAuditRecommendationsEn %>

1. **<%= desc.translations.integrateWithCicd %> / <%= desc.translations.integrateWithCicdEn %>**：
   - <%= desc.translations.integrateAuditIntoCicd %>
   - <%= desc.translations.runAuditBeforeDeploy %>

2. **<%= desc.translations.manualAuditsRegularly %> / <%= desc.translations.manualAuditsRegularlyEn %>**：
   - <%= desc.translations.runManualAudits %>
   - <%= desc.translations.newVulnerabilitiesFound %>

3. **<%= desc.translations.followSecurityAnnouncements %> / <%= desc.translations.followSecurityAnnouncementsEn %>**：
   - <%= desc.translations.followDependenciesUpdates %>
   - <%= desc.translations.useSecurityMonitoring %>